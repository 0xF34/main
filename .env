# No environment variables required for this project.
# The site is 100% static + client-side. Discord presence uses the
# public Lanyard API (no key needed). Deploy to Vercel/Cloudflare/Render
# with zero config.
